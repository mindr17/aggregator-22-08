{
  "name": "ozone",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx ts-node-dev --respawn --transpile-only ./src/index.ts",
    "build": "esbuild ./src/index.ts --bundle --outfile=bundle.js --platform=node --format=esm"
  },
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "esbuild": "^0.14.48",
    "express": "^4.17.1",
    "express-validator": "^6.14.0"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13"
  }
}
